<Window x:Class="PlanetDetstvaAppWPF.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Корзина" Height="500" Width="800" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Ваш заказ" FontSize="20" FontWeight="Bold" Margin="5"/>

        <DataGrid x:Name="dgvOrder" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="False" Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул" Binding="{Binding Article}" Width="100"/>
                <DataGridTextColumn Header="Наименование" Binding="{Binding ProductName}" Width="250"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding FinalPrice, StringFormat={}{0:F2} ₽}" Width="100"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalPrice, StringFormat={}{0:F2} ₽}" Width="100"/>
                <DataGridTemplateColumn Header="Действие" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Удалить" Click="RemoveItem_Click" Tag="{Binding ProductID}" Height="25" Width="70"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
            <TextBlock Text="Сумма:" FontWeight="Bold" Margin="5"/>
            <TextBlock x:Name="lblTotal" Text="0 ₽" Margin="5"/>
            <TextBlock Text="Скидка:" FontWeight="Bold" Margin="5"/>
            <TextBlock x:Name="lblDiscount" Text="0 ₽" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="5">
            <TextBlock Text="Пункт выдачи:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="cmbPickupPoint" DisplayMemberPath="FullAddress" SelectedValuePath="PickupPointID" Width="300" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button x:Name="btnSave" Content="Оформить заказ" Width="150" Height="35" Margin="5" Click="BtnSave_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Height="35" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
